### waiting for completion of hook batch/Job/longhorn-pre-upgrade

longhorn nó không chịu lên mà cứ bị quay vòng vòng ở cái này. Vô event thì serviceaccount `longhorn-service-account` not found???

Đi vòng vòng kiếm `kubectl get sa -n longhorn-system` thì thấy "longhorn-service-account" chưa lên vậy sao thằng upgrade cứ đi kiếm? 

Đi coi cái job `longhorn-pre-upgrade` thì thấy nó chạy ở hook PreSync? nhìn tên là biết trước khi sync chạy trước. Mà trước khi sync chạy thì thằng `longhorn-service-account` ở sync làm gì có cho nó gọi? Thế là nó đứng chờ thằng kia lên xong quay hoài.

---

### Nhờ chatgpt mò lên github coi có issue gì không?

Phát hiện nhiều người cũng bị và họ kêu tắt cái job `longhorn-pre-upgrade` đi. thế là vô values.yaml thêm biến để tắt. 

Chổ này mới mệt nè. lúc thêm lại để ở cấp bên ngoài.

```
preUpgradeChecker:
    jobEnabled: false
```

nếu đúng phải là 

```
longhorn:
  preUpgradeChecker:
    jobEnabled: false
```

Do nó gọi là dependency gì đó nên phải truyền theo đúng tên ghi trong Chart.yaml. Nếu muốn đổi tên thì thêm alias.

```
dependencies:
  - name: longhorn
    repository: https://charts.longhorn.io
    version: 1.12.0
```

### bắt đầu mệt. ArgoCD bị đứng.

Phát hiện cái `longhorn-pre-upgrade` đứng cứng ngắt, xóa cái gì liên quan đến nó cũng không được. Làm mấy cái khác không lên theo.

hỏi chatgpt 1 hồi thì lòi ra cái cấu hình này của job

```
finalizers:
- argocd.argoproj.io/hook-finalizer
```

k8s sẽ ngồi đợi argocd xử lý xong cái này và tự gở cái finalizers: đi mới động vô được.

Đơn giản thôi lên argocd terminate cái job đó luôn. 

### Bước cuối tưởng như xong rồi.

Mới terminate xong rồi đi uống nước và đi tắm, lát sau vô lại thấy nó vẫn nằm ở đó ??? 

Lại đi debug 1 hồi thì thấy như cũ? what the?

Mệt quá terminate tới chừng nào hết lên nữa thì thôi. Sau vài lần lại được?

Thì ra trước đó commit thử nghiệm và sai mấy lần nên k8s chạy tuần tự lại mấy lần đó.